function ErrorPage() {
    return (
        <h1 style={{ color: "white", textAlign: "center", marginTop: "50px" }}>
            Error Page that Harish will build later (do something)
        </h1>
    );
}

export default ErrorPage;
